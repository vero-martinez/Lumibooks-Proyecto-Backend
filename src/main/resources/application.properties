spring.application.name=lumibooks-backend

# Activar el perfil de desarrollo para usar las propiedades específicas de desarrollo
spring.profiles.active=dev

# Configuración de JPA para validar el esquema de la base de datos
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true